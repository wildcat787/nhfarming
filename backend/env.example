# 🏠 NHFarming Local Development Environment
# Copy this file to .env and customize for your local setup

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration (Change this to a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL (for CORS - keep as localhost for local development)
FRONTEND_URL=http://localhost:3000

# Database Configuration (SQLite - local file)
DB_PATH=./farm.db

# Google Drive Integration (for database sync and backup)
# Follow GOOGLE_DRIVE_SETUP.md for detailed setup instructions
# GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id_here
# GOOGLE_APPLICATION_CREDENTIALS=google-credentials.json
# AUTO_SYNC_ENABLED=true

# Optional: Google Maps API Key (for field mapping features)
# Get your API key from: https://console.cloud.google.com/
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Optional: Email Configuration (for password reset, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Weather Station Configuration (Ecowitt)
# Local Network Access (recommended for faster data)
# ECOWITT_LOCAL_URL=http://YOUR_STATION_IP
# Example: ECOWITT_LOCAL_URL=http://192.168.1.100

# Cloud API Access (fallback if local doesn't work)
# Get these from: https://api.ecowitt.net/
# ECOWITT_APP_KEY=your_application_key
# ECOWITT_USER_API_KEY=your_api_key
# ECOWITT_DEVICE_MAC=your_device_mac

# Note: You can use either local network OR cloud API, or both (local preferred)

# Optional: Weather Location for Fallback Data
# Set your latitude and longitude for more accurate fallback weather
# Find your coordinates at: https://www.latlong.net/
# WEATHER_LATITUDE=-34.0222563
# WEATHER_LONGITUDE=137.8448953 